## Summary

<!-- What changed and why. Match existing launch()/persona behavior. -->

## Test plan

- [ ] `python -m pytest -m 'not slow'`
- [ ] No JS fingerprint inject / `addInitScript` stealth
- [ ] Personas unchanged unless this PR is about personas (no `macos-chrome`)
